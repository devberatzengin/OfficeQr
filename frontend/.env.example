# Boş bırak: Vite dev server proxy'si (vite.config.ts) backend'e yönlendirir,
# CORS derdi olmaz. Sadece Vite dışında farklı bir şekilde build/deploy
# edeceksen (ve backend'de CORS açıksa) tam URL ver, ör:
# VITE_API_URL=http://localhost:5188
VITE_API_URL=
